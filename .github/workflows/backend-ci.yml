name: Backend CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  ci-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 🔥 package-lock.json 불일치 체크
      - name: Check lockfile sync
        run: |
          npm install --package-lock-only
          git diff --exit-code package-lock.json || \
          (echo "❌ package-lock.json is not in sync with package.json" && exit 1)

      # 🔧 의존성 설치
      - name: Install Dependencies
        run: npm ci

      # 🔍 린트나 테스트 있다면 실행 (없으면 자동 PASS)
      - name: Run Tests
        run: npm test --if-present
